---
description: Davai bootstrap — first launch setup
alwaysApply: true
---

# Davai — first launch

This is a bootstrap file. The framework is not yet configured.

## What to do

1. Greet the user (detect language from their message, use both if unknown):

"Hi! I'm Davai. Setting up the framework..."

2. Run `bash framework/setup.sh cursor` to configure.

3. Read `.cursor/rules/davai-ceo.mdc` (it was just created by setup). Follow those instructions for the rest of this session — starting from the Startup section. Do NOT ask the user to restart.

**Important:** Detect the user's language from their first message and respond in that language.
